@using Perficient.Web.Features.Navigation.ViewModels
@using Newtonsoft.Json
@using Newtonsoft.Json.Serialization
@model Perficient.Web.Features.Navigation.ViewModels.FooterContainerViewModel

@*@if (Html.IsInEditMode())
{
    <div>
        <button onclick="toggleFooterJson()">Toggle JSON View</button>
        <pre id="json-footer" style="display:none;height:500px;overflow-y:scroll;background:#ddd;">
            @JsonConvert.SerializeObject(Model.Footer, new JsonSerializerSettings { ContractResolver = new CamelCasePropertyNamesContractResolver(), StringEscapeHandling = StringEscapeHandling.EscapeHtml })
        </pre>
    </div>

    <script>
        (function () {
            var element = document.getElementById("json-footer");
            var obj = JSON.parse(element.innerText);
            element.innerHTML = JSON.stringify(obj, undefined, 2);
        })();

        function toggleFooterJson() {
            var element = document.getElementById("json-footer");
            element.style.display = element.style.display === "none" ? "block" : "none";
        }
    </script>
}*@

@Html.ReactComponentFor(nameof(FooterContainerViewModel), ReactComponents.Footer, Model.Footer, "footer")
