import PropTypes from 'prop-types';

const props = {
  sectionHeaderStyles: PropTypes.arrayOf(PropTypes.string),
  h2: PropTypes.string,
  h3: PropTypes.string,
  h4: PropTypes.string,
  imageSrc: PropTypes.string,
  altText: PropTypes.string,
};

export default props;
